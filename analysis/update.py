from evaluation import *

import datetime

# indexs = scale_index + primary_industry_index + other_industry_index + other_subject_index + style_index + overseas_index

# for index in indexs:
#     update_to_lastest(index, 'day')
#     update_to_lastest(index, 'month')

# for index in indexs:
#     local_recalculate(index, 'day')

update_to_lastest('931791.CSI', 'day')
update_to_lastest('931791.CSI', 'month')